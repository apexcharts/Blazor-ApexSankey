{
  "name": "blazor-apexsankey-e2e",
  "private": true,
  "type": "module",
  "description": "Headless browser smoke + interaction test for the Blazor-ApexSankey demo. Guards against interop-only regressions (a diagram that builds and serializes cleanly but fails to render in the browser) whenever the apexsankey core is bumped.",
  "scripts": {
    "test": "node smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
